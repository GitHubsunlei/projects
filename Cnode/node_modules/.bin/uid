#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_uid@0.0.2@uid/bin/uid" "$@"
  ret=$?
else 
  node  "$basedir/../_uid@0.0.2@uid/bin/uid" "$@"
  ret=$?
fi
exit $ret
